import 'package:auto_size_text/auto_size_text.dart';import 'package:flutter/material.dart';import 'package:flutter_smart_dialog/flutter_smart_dialog.dart';class DisabledGeoTipsWidget extends StatelessWidget {  const DisabledGeoTipsWidget({super.key});  @override  Widget build(BuildContext context) {    return SizedBox(      height: MediaQuery.of(context).size.height*0.4,      width: MediaQuery.of(context).size.width*0.8,      child: Padding(        padding: EdgeInsets.all(5),        child: SingleChildScrollView(          child: Column(            children: [              SizedBox(height: 10,),              Image.asset(                "assets/images/tips.jpg",                width: double.infinity,              ),              SizedBox(height: 10,),              Text("You have rejected the access location. In the future, you can enable it through settings"),              SizedBox(height: 10,),              SizedBox(                width: double.infinity,                child: OutlinedButton(onPressed: (){SmartDialog.dismiss();}, child: AutoSizeText("Close")),              )            ],          ),        ),      ),    );  }}